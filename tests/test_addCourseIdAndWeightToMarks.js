const { expect, assert } = require('chai');
const { calculateStudentAvg } = require('../app.js');


describe("addCourseAndWeightToMarks function", () => {

  it("It should add course weight and id keys to the second arg", () => {
      expect([1, 2, 3]).to.have.lengthOf(3)
  });

  it("It should add course weight and id keys to the second arg", () => {
      expect([1, 2, 3]).to.have.lengthOf(3)
  });
  it("It should add course weight and id keys to the second arg", () => {
      expect([1, 2, 3]).to.have.lengthOf(3)
  });
});
